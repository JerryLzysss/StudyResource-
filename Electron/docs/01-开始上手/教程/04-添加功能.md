[TOC]

# 添加功能

> 官方文档：[Adding Features | Electron](https://www.electronjs.org/zh/docs/latest/tutorial/tutorial-4-adding-features)

这是 Electron 教程的第 4 部分。

## 概述

在前几部分中，你已经创建了一个基本的 Electron 应用。本部分将介绍如何向应用添加更多功能，例如：

- 原生菜单
- 对话框
- 系统托盘
- 通知

## 添加应用菜单

使用 `Menu` 模块创建应用菜单：

```js
const { Menu } = require('electron')

const template = [
  {
    label: '文件',
    submenu: [
      { role: 'quit', label: '退出' }
    ]
  },
  {
    label: '编辑',
    submenu: [
      { role: 'undo', label: '撤销' },
      { role: 'redo', label: '重做' },
      { type: 'separator' },
      { role: 'cut', label: '剪切' },
      { role: 'copy', label: '复制' },
      { role: 'paste', label: '粘贴' }
    ]
  }
]

const menu = Menu.buildFromTemplate(template)
Menu.setApplicationMenu(menu)
```

## 打开文件对话框

```js
const { dialog } = require('electron')

ipcMain.handle('dialog:openFile', async () => {
  const { canceled, filePaths } = await dialog.showOpenDialog()
  if (!canceled) {
    return filePaths[0]
  }
})
```

## 更多功能示例

更多常见功能的实现示例，请参阅 [示例章节](../../04-示例/示例预览.md)：

- [通知](../../04-示例/通知.md)
- [键盘快捷键](../../04-示例/键盘快捷键.md)
- [原生文件拖放](../../04-示例/原生文件拖放.md)
- [深色模式](../../04-示例/深色模式.md)

## 下一步

继续阅读 [05 打包程序](./05-打包程序.md)。
