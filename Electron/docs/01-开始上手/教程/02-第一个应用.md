
# 第一个应用

> 官方文档：[Building Your First App | Electron](https://www.electronjs.org/zh/docs/latest/tutorial/tutorial-2-first-app)

这是 Electron 教程的第 2 部分。

## 1. 运行应用

```js
// main.js
console.log('Hello from Electron 👋')
```

```json
// package.json
{
  "name": "my-electron-app",
  "version": "1.0.0",
  "description": "Hello World!",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jane Doe",
  "license": "MIT",
  "devDependencies": {
    "electron": "23.1.3"
  }
}
```

通过这种方式，执行 `npm run start` 就能够进行运行。

## 2. 网页装载到 BrowserWindow

```html
<!-- index.html -->
<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta
      http-equiv="Content-Security-Policy"
      content="default-src 'self'; script-src 'self'"
    />
    <meta
      http-equiv="X-Content-Security-Policy"
      content="default-src 'self'; script-src 'self'"
    />
    <title>Hello from Electron renderer!</title>
  </head>
  <body>
    <h1>Hello from Electron renderer!</h1>
    <p>👋</p>
  </body>
</html>
```

```js
// main.js
const { app, BrowserWindow } = require('electron')

const createWindow = () => {
  const win = new BrowserWindow({
    width: 800,
    height: 600
  })

  win.loadFile('index.html')
}

app.whenReady().then(() => {
  createWindow()
})
```

## 下一步

- 深入理解代码逻辑，请参阅 [02 第一个应用 - 解析](./02-第一个应用-解析.md)
- 继续教程，请参阅 [03 预加载脚本](./03-预加载脚本.md)
