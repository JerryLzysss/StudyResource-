# Web 嵌入

> 官方文档：[Web Embeds | Electron](https://www.electronjs.org/zh/docs/latest/tutorial/web-embeds)

在 Electron 应用中嵌入 Web 内容的方式：`BrowserView`、`WebContentsView`、`<webview>` 标签。

> **安全提示**：推荐使用 `BrowserView` 或 `WebContentsView`，避免使用 `<webview>` 标签。
