# 通知

> 官方文档：[Notifications | Electron](https://www.electronjs.org/zh/docs/latest/tutorial/notifications)

使用 `Notification` 模块显示系统通知。

```js
const { Notification } = require('electron')

new Notification({
  title: '标题',
  body: '通知内容'
}).show()
```
