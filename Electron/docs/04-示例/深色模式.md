# 深色模式

> 官方文档：[Dark Mode | Electron](https://www.electronjs.org/zh/docs/latest/tutorial/dark-mode)

使用 `nativeTheme` 模块检测和响应系统深色模式设置。

```js
const { nativeTheme } = require('electron')
nativeTheme.themeSource = 'dark' // 'system' | 'light' | 'dark'
```
