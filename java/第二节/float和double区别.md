# float 和 double

1. `float` 的尾数约为 23 位二进制，十进制有效精度大约 6～7 位
2. `double` 的尾数约为 52 位二进制，十进制有效精度大约 15～16 位

因此一般优先使用 `double`；只有明确需要节省内存或对接特定 API 时才用 `float`。

## 变量使用注意

1. 局部变量不初始化不能直接使用
2. 同一个作用域（一对大括号）内不能重复定义同名变量
3. 外层作用域不能访问内层局部变量；内层可以访问外层变量
