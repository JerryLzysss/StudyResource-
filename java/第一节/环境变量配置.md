# Java 环境变量配置

1. 打开：此电脑 → 属性 → 高级系统设置 → 环境变量
2. 在「系统变量」中**新建**变量：
   - 变量名：`JAVA_HOME`
   - 变量值：JDK 安装根目录（`bin` 文件夹的上一级，例如 `C:\Program Files\Java\jdk-17`）
3. 编辑系统变量 `Path`，新增一项：`%JAVA_HOME%\bin`
4. 打开新的 CMD 窗口，执行 `java -version` 和 `javac -version` 验证是否配置成功
