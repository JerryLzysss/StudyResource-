# 抽象类

意义：把共有行为提取到父类中；若父类中某方法尚不确定具体实现，可只写方法声明、不写方法体（抽象方法）。含有抽象方法的类必须是抽象类。

关键字：`abstract`

- 抽象方法：`abstract 返回值类型 方法名(参数);`
- 抽象类：`public abstract class 类名 { }`

规则：

- 抽象方法所在的类一定是抽象类
- 抽象类中可以没有抽象方法
- 子类继承抽象类后，必须重写父类中所有抽象方法（除非子类自己也是抽象类）
- 抽象类不能直接 `new`，只能创建其子类对象，再通过多态等方式调用
