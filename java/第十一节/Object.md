# Object

所有类都直接或间接继承 `Object` 类。

## toString

若不重写，默认返回类似 `类名@哈希码十六进制` 的字符串（常被说成「地址信息」）。  
若希望直接打印对象时看到有意义内容，应重写 `toString()`。

## equals

比较两个对象内容是否相等时，通常需要重写 `equals()`（并建议同时重写 `hashCode()`）。

## clone

通过 `clone()` 复制对象时，默认是**浅拷贝**：复制后字段值相同，但引用类型字段仍指向同一对象。  
若需要深拷贝，需自行实现（或改用拷贝构造、序列化等方式）。
