参考文档: [环境变量](https://jest.nodejs.cn/docs/environment-variables/)

# 环境变量
Jest 设置以下环境变量：

### NODE_ENV

如果尚未设置为其他值，则设置为 `'test'`。

### JEST_WORKER_ID

每个工作进程都分配有一个唯一的 ID（基于索引，以 `1` 开头）。当 [`runInBand`](https://jest.nodejs.cn/docs/cli#--runinband) 设置为 true 时，所有测试都将其设置为 `1`。
