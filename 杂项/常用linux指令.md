# 【Linux】Linux常用命令（含完整命令语句）

---

## 一、文件和目录操作（12条）

| 序号 | 命令 | 功能说明 | 完整命令语句示例 |
|:----:|------|----------|----------------|
| 1 | `ls` | 列出目录内容 | `ls -la` / `ls -lh` / `ls --color=auto` |
| 2 | `pwd` | 显示当前工作目录 | `pwd` |
| 3 | `cd` | 切换工作目录 | `cd ~` / `cd ..` / `cd -` |
| 4 | `mkdir` | 创建新目录 | `mkdir 目录名` / `mkdir -p 父/子目录` |
| 5 | `rmdir` | 删除空目录 | `rmdir 目录名` / `rmdir -p 父/子目录` |
| 6 | `rm` | 删除文件或目录 | `rm 文件名` / `rm -r 目录` / `rm -rf 强制删除` |
| 7 | `cp` | 复制文件或目录 | `cp 源文件 目标文件` / `cp -r 源目录 目标目录` |
| 8 | `mv` | 移动或重命名 | `mv 旧名 新名` / `mv 文件 目录/` |
| 9 | `touch` | 创建空文件或更新时间戳 | `touch 新文件` / `touch -t 202301011200 文件` |
| 10 | `find` | 查找文件 | `find / -name "*.txt"` / `find . -type f -size +100M` |
| 11 | `locate` | 快速查找文件（需updatedb） | `locate 文件名` |
| 12 | `tree` | 以树状结构显示目录 | `tree` / `tree -L 2`（显示2层） |

---

## 二、文件内容查看（10条）

| 序号 | 命令 | 功能说明 | 完整命令语句示例 |
|:----:|------|----------|----------------|
| 13 | `cat` | 显示文件全部内容 | `cat 文件名` / `cat -n 文件名`（显示行号） |
| 14 | `tac` | 倒序显示文件内容 | `tac 文件名` |
| 15 | `more` | 分页显示（只能向下翻） | `more 文件名` |
| 16 | `less` | 分页显示（可上下翻页） | `less 文件名` / `less +F 日志文件`（实时） |
| 17 | `head` | 显示文件前N行 | `head -n 20 文件名` / `head -c 100 文件名` |
| 18 | `tail` | 显示文件后N行 | `tail -n 20 文件名` / `tail -f 日志文件`（实时） |
| 19 | `nl` | 显示文件并添加行号 | `nl 文件名` / `nl -ba 文件名` |
| 20 | `wc` | 统计文件行数/字数/字节 | `wc -l 文件名`（行数）/ `wc -w 文件名`（字数） |
| 21 | `file` | 查看文件类型 | `file 文件名` |
| 22 | `stat` | 显示文件详细信息 | `stat 文件名` |

---

## 三、文本搜索与处理（10条）

| 序号 | 命令 | 功能说明 | 完整命令语句示例 |
|:----:|------|----------|----------------|
| 23 | `grep` | 文本搜索过滤 | `grep "关键字" 文件` / `grep -r "关键字" 目录/` / `grep -i "关键字"`（忽略大小写） |
| 24 | `sed` | 流编辑器（文本替换） | `sed -i 's/旧/新/g' 文件` / `sed -n '5,10p' 文件`（显示5-10行） |
| 25 | `awk` | 文本处理工具 | `awk '{print $1}' 文件` / `awk -F: '{print $1}' /etc/passwd` |
| 26 | `cut` | 截取字段 | `cut -d: -f1 /etc/passwd` / `cut -c1-5 文件` |
| 27 | `sort` | 排序 | `sort 文件` / `sort -r 文件`（倒序）/ `sort -n 文件`（数字排序） |
| 28 | `uniq` | 去除重复行 | `uniq 文件` / `uniq -c 文件`（统计重复次数） |
| 29 | `diff` | 比较文件差异 | `diff 文件1 文件2` / `diff -u 文件1 文件2` |
| 30 | `echo` | 输出文本 | `echo "内容"` / `echo "内容" > 文件` / `echo -e "第1行\n第2行"` |
| 31 | `tee` | 输出到屏幕和文件 | `echo "内容" | tee 文件` / `命令 | tee -a 文件`（追加） |
| 32 | `xargs` | 将标准输入转为参数 | `cat 文件 | xargs rm` / `find . -name "*.log" | xargs rm` |

---

## 四、系统信息与进程管理（10条）

| 序号 | 命令 | 功能说明 | 完整命令语句示例 |
|:----:|------|----------|----------------|
| 33 | `ps` | 查看进程状态 | `ps aux` / `ps -ef` / `ps aux | grep 进程名` |
| 34 | `top` | 实时显示进程 | `top` / `top -p PID`（监控指定进程） |
| 35 | `htop` | 交互式进程查看器（需安装） | `htop` |
| 36 | `kill` | 终止进程 | `kill PID` / `kill -9 PID`（强制终止） / `killall 进程名` |
| 37 | `df` | 查看磁盘空间 | `df -h`（人类可读）/ `df -T`（显示文件系统类型） |
| 38 | `du` | 查看目录/文件大小 | `du -sh 目录` / `du -h --max-depth=1` |
| 39 | `free` | 查看内存使用 | `free -h` / `free -m` |
| 40 | `uptime` | 查看系统运行时间 | `uptime` |
| 41 | `uname` | 查看系统信息 | `uname -a`（全部信息）/ `uname -r`（内核版本） |
| 42 | `dmesg` | 查看系统启动信息 | `dmesg` / `dmesg | tail -20` |

---

## 五、用户与权限管理（6条）

| 序号 | 命令 | 功能说明 | 完整命令语句示例 |
|:----:|------|----------|----------------|
| 43 | `chmod` | 修改文件权限 | `chmod 755 文件` / `chmod u+x 文件` / `chmod -R 777 目录` |
| 44 | `chown` | 修改文件所有者 | `chown 用户 文件` / `chown 用户:组 文件` / `chown -R 用户 目录` |
| 45 | `sudo` | 以超级用户权限执行 | `sudo 命令` / `sudo -i`（切换到root） |
| 46 | `su` | 切换用户 | `su - 用户名` / `su -`（切换到root） |
| 47 | `whoami` | 显示当前用户 | `whoami` |
| 48 | `id` | 显示用户ID和组ID | `id` / `id 用户名` |

**权限数字说明：**
- 7 = rwx（读+写+执行）
- 6 = rw-（读+写）
- 5 = r-x（读+执行）
- 4 = r--（只读）
- 用户/组/其他权限顺序：如 755 = 用户7、组5、其他5

---

## 六、压缩与解压（6条）

| 序号 | 命令 | 功能说明 | 完整命令语句示例 |
|:----:|------|----------|----------------|
| 49 | `tar` | 打包/解包文件 | `tar -cvf 包.tar 文件` / `tar -xvf 包.tar` |
| 50 | `tar.gz` | gzip压缩/解压 | `tar -czvf 包.tar.gz 文件` / `tar -xzvf 包.tar.gz` |
| 51 | `tar.bz2` | bzip2压缩/解压 | `tar -cjvf 包.tar.bz2 文件` / `tar -xjvf 包.tar.bz2` |
| 52 | `gzip` | gzip压缩 | `gzip 文件` / `gzip -d 文件.gz`（解压） |
| 53 | `zip` | zip压缩 | `zip 包.zip 文件` / `zip -r 包.zip 目录` |
| 54 | `unzip` | zip解压 | `unzip 包.zip` / `unzip 包.zip -d 目标目录` |

---

## 七、网络相关（6条）

| 序号 | 命令 | 功能说明 | 完整命令语句示例 |
|:----:|------|----------|----------------|
| 55 | `ping` | 测试网络连通性 | `ping baidu.com` / `ping -c 4 192.168.1.1`（发送4次） |
| 56 | `ifconfig` / `ip` | 查看/配置网络接口 | `ifconfig` / `ip addr` / `ip link` |
| 57 | `netstat` / `ss` | 查看网络连接 | `netstat -tlnp` / `ss -tlnp` |
| 58 | `curl` | 下载/请求URL | `curl http://网址` / `curl -O 文件URL` / `curl -L 重定向网址` |
| 59 | `wget` | 下载文件 | `wget 文件URL` / `wget -c 文件URL`（断点续传） |
| 60 | `ssh` | 远程登录 | `ssh 用户@主机` / `ssh -p 端口 用户@主机` |

---

## 附录：常用组合命令

```bash
# 1. 查找并删除某类文件
find . -name "*.tmp" -type f -delete

# 2. 统计文件行数并按数量排序
cat 文件 | sort | uniq -c | sort -nr

# 3. 实时监控日志并过滤关键字
tail -f 日志文件 | grep "错误"

# 4. 批量重命名（将所有.txt改为.bak）
for f in *.txt; do mv "$f" "${f%.txt}.bak"; done

# 5. 查找大文件（大于100M）
find / -type f -size +100M -exec ls -lh {} \;

# 6. 查看端口占用
netstat -tlnp | grep :80

# 7. 杀死所有某类进程
ps aux | grep "进程名" | grep -v grep | awk '{print $2}' | xargs kill -9

# 8. 批量修改文件权限
find . -type f -exec chmod 644 {} \;
find . -type d -exec chmod 755 {} \;

# 9. 创建软链接
ln -s 源文件 链接名

# 10. 后台运行程序并记录日志
nohup 命令 > 输出.log 2>&1 &
```

---

## 快捷键速查表

| 快捷键 | 功能 |
|--------|------|
| `Tab` | 自动补全命令/文件名 |
| `Ctrl+C` | 终止当前进程 |
| `Ctrl+Z` | 暂停当前进程（fg恢复） |
| `Ctrl+D` | 退出当前终端 |
| `Ctrl+L` | 清屏（等同于clear） |
| `Ctrl+A` | 光标移到行首 |
| `Ctrl+E` | 光标移到行尾 |
| `Ctrl+U` | 删除光标前所有字符 |
| `Ctrl+K` | 删除光标后所有字符 |
| `Ctrl+R` | 搜索历史命令 |
| `!!` | 执行上一条命令 |
| `!n` | 执行历史第n条命令 |
| `!$` | 上条命令的最后一个参数 |
